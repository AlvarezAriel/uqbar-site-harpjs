header#header.alt
    h1#logo
        a(href='index.html') #{title}
    nav#nav
        ul
            each section in public._data.sections
                li: a(href="#{ section.path }") #{section.label}
